{
  "name": "magic-pro-monorepo",
  "version": "1.0.0",
  "private": true,
  "description": "Magic Pro Monorepo Workspace (Frontend + Backend)",
  "scripts": {
    "dev:frontend": "cd frontend && npm run dev",
    "dev:backend": "cd backend && npm start",
    "build:frontend": "cd frontend && npm run build"
  }
}
